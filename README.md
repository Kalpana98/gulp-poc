# Gulp

This repository mocks a bundler using Gulp. I have a _`src/`_ folder that I'd
like to serve. Gulp creates a minified production build (_`dist/`_) of it.
Checkout [gulpfile.js](gulpfile.js).

To run locally -

`npm i`

`gulp`

In watch mode -

`gulp watch`
